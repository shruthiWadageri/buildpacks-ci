---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfbuildpacks/ci

inputs:
  - name: buildpacks-ci

run:
  path: buildpacks-ci/tasks/run-shellcheck/run.sh

params:
  DIRECTORY_TO_CHECK:
